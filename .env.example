PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/ais_concepts
JWT_SECRET=change-this-to-a-long-random-string-in-production
# CORS: use your frontend origin when API is on a different host/port. Comma-separated for multiple.
# Same host as API (this repo’s default): http://localhost:3000
CLIENT_ORIGIN=http://localhost:3000
